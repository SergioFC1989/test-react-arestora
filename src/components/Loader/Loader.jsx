import React from "react";

import "./Loader.scss";

/**
 * Loader component that renders a span with a loader class.
 *
 * @returns {JSX.Element} A span element with the class "loader".
 */
export const Loader = () => <span className="loader"></span>;
